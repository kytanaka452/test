# Claude Code 基本ルール

このファイルには、このプロジェクトでClaude Codeを使用する際の基本的なルールとガイドラインを記述しています。

## コーディング規約

### 一般原則
- シンプルで読みやすいコードを書く
- 過度な抽象化や過剰設計を避ける
- 明示的に要求されていない機能は追加しない
- 既存のコードスタイルに従う

### コメントとドキュメント
- ロジックが自明でない場合のみコメントを追加
- 変更していないコードにはドキュメントを追加しない
- コードの「何を」ではなく「なぜ」を説明する

### セキュリティ
- コマンドインジェクション、XSS、SQLインジェクションなどの脆弱性を防ぐ
- ユーザー入力や外部APIなど、システム境界でのみバリデーションを行う
- 内部コードやフレームワークの保証を信頼する

## ファイル操作

### 優先順位
1. **編集優先**: 新規ファイル作成より既存ファイルの編集を優先
2. **必要最小限**: 明示的に必要な場合のみファイルを作成
3. **マークダウンファイル**: 特に要求されない限り、ドキュメントやREADMEファイルは作成しない

### コード変更
- コードを読む前に変更を提案しない
- 既存のコードを理解してから修正を行う
- 要求された変更に直接関係する部分のみを修正

## エラー処理

- 実際に起こり得るシナリオに対してのみエラー処理を追加
- 不要なフォールバックやバリデーションは追加しない
- 内部コードには過度な防御的プログラミングを行わない

## 後方互換性

- 未使用のコードは完全に削除する
- `_var`のような未使用変数のリネームは避ける
- 削除されたコードに`// removed`コメントを残さない
- 不要な再エクスポートやハックは使用しない

## Git操作

### コミット作成時
- ユーザーが明示的に要求した場合のみコミットを作成
- 変更の「何を」ではなく「なぜ」に焦点を当てた簡潔なメッセージ
- フックをスキップしない（`--no-verify`等を使用しない）
- 機密情報を含むファイル（.env、credentials.json等）はコミットしない

### 禁止事項
- git configの変更
- force pushやhard resetなどの破壊的コマンド（明示的に要求された場合を除く）
- main/masterブランチへのforce push

## テストコード作成時の厳守事項
### 絶対に守ってください‼

### テストコードの品質
- テストは必ず実際の機能を検証すること
- 'expect(true).toBe(true)'のような意味のないアサーションは絶対に書かない
- 各テストケースは具体的な入力と期待される出力を検証すること
- モックは必要最小限にとどめ、実際の動作に近い形でテストすること

### ハードコーディングの禁止
- テストを通すためだけのハードコーディングは厳禁
- 本番コードに'if(testMode)'のような条件分岐を書かない
- テスト用の特別な値（マジックナンバー）を本番コードに組み込まない
- 環境変数や設定ファイルを使用して、テスト環境と本番環境を適切に分離すること

### テスト実装の原則
- テストが失敗する状態から始めること（Red-Green-Refactor）
- 境界値、異常値、エラーケースも必ずテストすること
- カバレッジだけでなく、実際の品質を重視すること
- テストケース名は何をテストしているのか明確に記述すること

### 実装する前の確認
- 機能の仕様を正しく理解してからテストを書くこと
- 不明なテストがあれば、仮の実装ではなく、ユーザーに確認すること

## プロジェクト固有のルール

この下に、このプロジェクト特有のルールや規約を追加してください。

### 例:
- 使用する言語/フレームワーク
- テスト要件
- コードレビュープロセス
- デプロイ手順